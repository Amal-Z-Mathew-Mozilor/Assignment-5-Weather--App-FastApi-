services:
  api: &main_app
    build:
      context: .
      target: dev
      dockerfile: ./Dockerfile
    image: fast_api:${FAST_API_VERSION:-latest}
    restart: always
    ports:
      # Exposes application port.
    - "8000:8000"
    env_file:
      - path: .env
        required: false
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    environment:
      FAST_API_HOST: 0.0.0.0
      FAST_API_RELOAD: "True"
      FAST_API_DB_HOST: fast_api-db
      FAST_API_DB_PORT: 5432
      FAST_API_DB_USER: fast_api
      FAST_API_DB_PASS: fast_api
      FAST_API_DB_BASE: fast_api
      FAST_API_REDIS_HOST: fast_api-redis
    volumes:
      - .:/app/src/

  db:
    image: postgres:18.3-trixie
    hostname: fast_api-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: "fast_api"
      POSTGRES_USER: "fast_api"
      POSTGRES_DB: "fast_api"
    volumes:
      - fast_api-db-data:/var/lib/postgresql
    restart: always
    healthcheck:
      test: pg_isready -U fast_api
      interval: 2s
      timeout: 3s
      retries: 40

  migrator:
    <<: *main_app
    restart: "no"
    command: alembic upgrade head
    depends_on:
      db:
        condition: service_healthy

  redis:
    image: redis:8.4.0
    hostname: "fast_api-redis"
    restart: always
    ports:
      - 6379:6379
    environment:
      ALLOW_EMPTY_PASSWORD: "yes"
    healthcheck:
      test: redis-cli ping
      interval: 1s
      timeout: 3s
      retries: 50



volumes:
  fast_api-db-data:
    name: fast_api-db-data
